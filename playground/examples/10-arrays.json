{
  "order": 10,
  "title": "Arrays",
  "snippet": "Fixed-size collections",
  "description": "Arrays store multiple values of the same type. Access elements using square brackets with zero-based indexing.",
  "code": "extern printf(fmt: string, ...);\n\nframe main() ret int {\n    # Declare an array of 5 integers\n    local numbers: int[5];\n    local i: int;\n    \n    # Initialize array elements\n    numbers[0] = 10;\n    numbers[1] = 20;\n    numbers[2] = 30;\n    numbers[3] = 40;\n    numbers[4] = 50;\n    \n    # Print array elements\n    printf(\"Array elements:\\n\");\n    i = 0;\n    loop (i < 5) {\n        printf(\"numbers[%d] = %d\\n\", i, numbers[i]);\n        i = i + 1;\n    }\n    \n    # Calculate sum\n    local sum: int;\n    sum = 0;\n    i = 0;\n    loop (i < 5) {\n        sum = sum + numbers[i];\n        i = i + 1;\n    }\n    printf(\"Sum: %d\\n\", sum);\n    \n    # Find maximum\n    local max: int;\n    max = numbers[0];\n    i = 1;\n    loop (i < 5) {\n        if (numbers[i] > max) {\n            max = numbers[i];\n        }\n        i = i + 1;\n    }\n    printf(\"Maximum: %d\\n\", max);\n    \n    return 0;\n}"
}
