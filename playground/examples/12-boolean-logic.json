{
  "order": 12,
  "title": "Boolean Logic",
  "snippet": "Logical operators and conditions",
  "description": "Use &&(and), ||(or), and !(not) for boolean logic. Combine conditions to create complex decision-making code.",
  "code": "extern printf(fmt: string, ...);\n\nframe main() ret int {\n    local age: int;\n    local hasLicense: bool;\n    local hasInsurance: bool;\n    \n    age = 20;\n    hasLicense = true;\n    hasInsurance = true;\n    \n    # AND operator (&&)\n    if (age >= 18 && hasLicense) {\n        printf(\"You can drive!\\n\");\n    }\n    \n    # OR operator (||)\n    if (age < 18 || !hasLicense) {\n        printf(\"Cannot drive alone\\n\");\n    } else {\n        printf(\"Can drive alone\\n\");\n    }\n    \n    # NOT operator (!)\n    if (!hasInsurance) {\n        printf(\"Get insurance first!\\n\");\n    }\n    \n    # Complex conditions\n    local score: int;\n    local attendance: int;\n    score = 85;\n    attendance = 95;\n    \n    if ((score >= 80 && attendance >= 90) || score >= 95) {\n        printf(\"Excellent performance!\\n\");\n    }\n    \n    # Boolean variables\n    local isWeekend: bool;\n    local isHoliday: bool;\n    isWeekend = false;\n    isHoliday = true;\n    \n    if (isWeekend || isHoliday) {\n        printf(\"Day off!\\n\");\n    } else {\n        printf(\"Work day\\n\");\n    }\n    \n    return 0;\n}"
}
