{
  "order": 35,
  "title": "Advanced Type Aliases",
  "snippet": "Create flexible type aliases including generics",
  "description": "Type aliases allow you to create custom names for types. You can create aliases for primitives, composites, and even generic types. This makes code more readable and maintainable.",
  "code": "# Simple type aliases\ntype Age = int;\ntype Score = float;\ntype Name = string;\n\nextern printf(fmt: string, ...);\n\nframe main() ret int {\n    # Use type aliases\n    local myAge: Age = 25;\n    local myScore: Score = 95.5;\n    local myName: Name = \"Alice\";\n    \n    printf(\"Name: %s, Age: %d, Score: %.1f\\n\", myName, myAge, myScore);\n    \n    # Work with simple types\n    local x: int = 10;\n    local y: int = 20;\n    printf(\"Values: x=%d, y=%d\\n\", x, y);\n    \n    # Create an array\n    local numbers: int[5] = [1, 2, 3, 4, 5];\n    local i: int = 0;\n    printf(\"Array: \");\n    loop (i < 5) {\n        printf(\"%d \", numbers[i]);\n        i = i + 1;\n    }\n    printf(\"\\n\");\n    \n    return 0;\n}"
}
