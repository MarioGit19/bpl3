# Inline Assembly

BPL allows embedding LLVM IR or assembly instructions directly for performance-critical sections.

## Syntax

_(Syntax for inline assembly is currently experimental)_

```bpl
# Example (conceptual)
asm {
    "mov rax, 1"
}
```
